version: 2

models:
  - name: int_contacts
    description: "Intermediate deduplicated table to contacts"
    columns:
      - name: contact_id
        tests:
          - unique
          - not_null

  - name: int_campaigns
    description: "Intermediate deduplicated table to campaigns"
    columns:
      - name: s_campaign_id
        tests:
          - unique
          - not_null

  - name: int_links
    description: "Intermediate deduplicated table to links"
    columns:
      - name: s_link_id
        tests:
          - unique
          - not_null

  - name: fact_unsubscribe
    description: "Intermediate deduplicated fact tables related to unsubscribe events"
    columns:
      - name: contact_id
        tests:
          - unique
          - not_null